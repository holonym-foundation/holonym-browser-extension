{
  "type": "module",
  "scripts": {
    "build": "rm -rf ./dist/* && cp ./manifest.json ./dist/manifest.json && cp ./src/content/*.css ./dist && rollup --config rollup.config.js"
  },
  "dependencies": {
    "@rollup/plugin-commonjs": "^22.0.2",
    "buffer": "^6.0.3",
    "ethers": "^5.6.9"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^13.3.0",
    "process": "^0.11.10",
    "rollup-plugin-babel": "^4.4.0"
  }
}
